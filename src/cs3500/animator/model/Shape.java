package cs3500.animator.model;

/**
 * An enum that represents a shape type. This enum can be expanded by simply adding another shape
 * type and then extending a class from AbstractShape and adding this as its field.
 */
public enum Shape {
  RECTANGLE, OVAL
}